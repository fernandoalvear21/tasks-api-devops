version: '3.8'
services:
  api:
    build: .
    ports:
      - 8000:8000
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=sqlite:///tasks.db
    command: ["/app/start.sh"]
    restart: unless-stopped
